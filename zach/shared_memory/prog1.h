#ifndef PROG1_H
#define PROG1_H

#include <vector>
#include <dirent.h>
#include <iostream>
#include <fstream>
#include <string.h>

using namespace std;

void cmdnm(vector<string> & );
void pid(vector<string> & );
void systat(vector<string> & );

#endif